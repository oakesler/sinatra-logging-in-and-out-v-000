<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8">
    <title>title</title>
    <link rel="stylesheet" href="stylesheets/bootstrap.css" type="text/css">  
  </head>
  <body id="page-top">
    
    <% if !!Helpers.current_user(session)%>
    
      <p>Welcome <%=Helpers.current_user(session).username%></p>
      <p>Welcome <%=Helpers.current_user(session).balance%></p>
      
    <% else %>
    
      <p>You Must <a href="/">Log In</a> to View Your Balance</p>
      
    <% end %>
  
  
  <a href="/logout">Log Out</a>
  </body>
</html>


<%# <% if Helpers.is_logged_in?(session) %>
    <%#  <p>Welcome <%=Helpers.current_user(session).username%><
    <%#  <p><%=Helpers.current_user(session).balance%></p>
 <%#   <% elsif%>
  <%#    <p>You Must <a href="/">Log In</a> to View Your Balance</p>
    <% end %>